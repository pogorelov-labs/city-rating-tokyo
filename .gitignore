node_modules/
.next/
out/
.env*
!.env.example
.DS_Store
__pycache__/
*.pyc
.venv/

# CRTKY-109 generation pipeline intermediate files
# Regenerate with: scripts/build-datamart.py, scripts/build-prompts-dir.py
data/station-datamart.json
data/prompts/
# data/descriptions/ IS committed — that's the output we want to keep
# data/generated-descriptions.json IS committed — that's the merged result

# MCP embeddings (built offline by scripts/build-embeddings.py, ~80 MB).
# Regenerated in the Docker stage 2 of mcp/Dockerfile, so the prod image
# never reads from git for this file.
data/embeddings.npz

# Playwright artifacts (root-level e2e harness). The HTML report and
# test-results dir are regenerated on every `npm run test:e2e` run.
test-results/
playwright-report/
playwright/.cache/
